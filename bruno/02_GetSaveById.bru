meta {
  name: Save Service - Get Save by ID
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/v1/save/1
  body: none
  auth: none
}

tests {
  test("Status is 200 or 404", function() {
    const status = res.getStatus();
    expect([200, 404]).to.include(status);
  });
}
