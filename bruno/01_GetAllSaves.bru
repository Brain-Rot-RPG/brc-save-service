meta {
  name: Save Service - Get All Saves
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/v1/save
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is array", function() {
    expect(res.getBody()).to.be.an('array');
  });
}
